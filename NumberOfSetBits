class Solution {
    public int countSetBits(int n) {
        int count = 0;
        while (n != 0) {
            count++;
            n &= (n - 1); // clears the lowest set bit
        }
        return count;
    }
}
